# Codex 聊天同步记录

用途：在 Codex++ 里切换供应商/模型后，如果聊天记录消失，让新的供应商先读取本文件，就能快速接上当前任务。

建议开场提示：

> 请先阅读当前目录下的 `Codex聊天同步记录.md`，按其中上下文继续，不要重新生成不需要的文件。

## 当前工作目录

`C:\Users\28097\Desktop\my_code\my_code\作业\信号与系统`

## 已识别文件

- `期末考试试卷-信号与系统.pdf`：第一套试卷。
- `21861fd21715f149a67d70cdc67e6976.jpg`：第一套答案长图。
- `answer_slices/`：第一套答案截图切片。
- `信号与系统第二套.pdf`：第二套试卷，内含试题页和参考答案页。
- `second_exam_pages/`：已从第二套 PDF 抽出的扫描页图片。

## 用户偏好

- 不需要额外生成详解 md，除非明确要求。
- 答案优先直接打在聊天里。
- 需要“详细、直观”的解析，尤其是图形题和频谱搬移题。
- 如果原答案有错误，需要指出并纠正。

## 第一套试卷重要纠错

第一套曾发现并核对过：

1. 第 2 大题反馈级联系统的部分分式系数不能都写成 `1/3`。
2. 第 5 大题零输入响应中，原答案若出现 `(2s+13)/((s+2)(s+3))`，不匹配本题；应按初值重新算。
3. 第 4 大题输出里的 `-cos(t)` 与 `sin(t-π/2)` 等价，不算错。
4. 第 6 大题系统函数 `(s+2)/((s+2)(s+3))` 可化简为 `1/(s+3)`，`s=-2` 是极零相消点。

## 第二套答案速记

约定：`Sa(x)=sin(x)/x`。

### 一、选择题

1. D
2. B
3. B
4. B
5. A

### 二、填空题

1. `1-e^{-j3ω0}`
2. `e^{-2t+2}`
3. 线性，时变
4. `f(t)=-f(t+T/2)`
5. 并联：`h1(t)+h2(t)`；级联：`h1(t)*h2(t)`
6. 离散非周期
7. `F(ω)=2πδ(ω)+2π[δ(ω-1)+δ(ω+1)]+jπ/2[δ(ω+3)-δ(ω-3)]`
8. `Fs(ω)=2/π Σ F(ω-4n)`
9. `F(jΩ)=2πΣFnδ(Ω-n2π/T)`
10. 左半平面

### 三、卷积

`f1(t)=u(t+1)-u(t-1)`，`f2(t)=δ(t+1)-δ(t-1)`。

`y(t)=f1(t)*f2(t)=f1(t+1)-f1(t-1)=u(t+2)-2u(t)+u(t-2)`。

分段：

```text
y(t)=0,   t<-2
y(t)=1,   -2<t<0
y(t)=-1,  0<t<2
y(t)=0,   t>2
```

### 四、傅里叶变换

`f(t)=[e^{-t}u(t)]*[u(t)-u(t-2)]`

```text
F(jΩ)=2Sa(Ω)e^{-jΩ}/(1+jΩ)
```

### 五、时移加调制

已知 `F(jΩ)=EτSa(Ωτ/2)`，求 `f1(t)=f(t-τ)cos(Ω0t)`：

```text
F1(jΩ)=1/2 Eτ e^{-j(Ω-Ω0)τ} Sa[(Ω-Ω0)τ/2]
      +1/2 Eτ e^{-j(Ω+Ω0)τ} Sa[(Ω+Ω0)τ/2]
```

### 六、反馈级联系统

`H1=1/(s+1)`，`H2=1/(s+3)`，`H3=1/(s+5)`。

```text
H(s)=H1H3/(1+H1H2)=(s+3)/[(s+2)^2(s+5)]
```

```text
h(t)=[2/9 e^{-2t}+1/3 t e^{-2t}-2/9 e^{-5t}]u(t)
```

### 七、微分方程

`r''+5r'+6r=e'+4e`，`r(0-)=1`，`r'(0-)=-1`，`e(t)=u(t)`。

```text
H(s)=(s+4)/[(s+2)(s+3)]
```

零点：`-4`；极点：`-2,-3`。

```text
h(t)=(2e^{-2t}-e^{-3t})u(t)
rzi(t)=(2e^{-2t}-e^{-3t})u(t)
rzs(t)=(2/3-e^{-2t}+1/3e^{-3t})u(t)
r(t)=(2/3+e^{-2t}-2/3e^{-3t})u(t)
rh(t)=(e^{-2t}-2/3e^{-3t})u(t)
rp(t)=2/3u(t)
```

### 八、抽样与矩形系统

```text
fs(t)=f(t)p(t)
Fs(ω)=1/Ts Σ F(ω-n·2π/Ts)
```

`h(t)=u(t+τ)-u(t-τ)`，所以

```text
H(ω)=2τSa(ωτ)
F0(ω)=Fs(ω)H(ω)
     =[2τSa(ωτ)/Ts]ΣF(ω-n·2π/Ts)
```

### 九、调制-滤波系统

输入频谱 `F(jω)` 是 `[-20,20]` 的半圆，峰值 1。

```text
A(ω)=π[δ(ω-1000)+δ(ω+1000)]
B(ω)=1/2[F(ω-1000)+F(ω+1000)]
```

经过高通 `H1` 后只保留 `|ω|>1000` 的外侧半圆。

再乘 `cos(1000t)`：

```text
D(ω)=1/2[C(ω-1000)+C(ω+1000)]
```

最后低通 `H2` 只保留 `[-20,20]` 的基带半圆：

```text
Y(jω)=1/4F(jω)
y(t)=1/4f(t)
```

## 推荐同步方法

1. 把本文件当作“跨供应商共享记忆”。
2. 每次完成一大段任务后，让 Codex 更新本文件。
3. 切换供应商后，第一句话让它先读本文件。
4. 如果任务较长，可以再建 `任务日志.md`，只记录最近决策、已完成项、待办项和关键公式。
