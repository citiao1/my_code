CODE SEGMENT
ASSUME CS:CODE
MAIN PROC FAR
START:  
	PUSH DS
	MOV AX,0 
	PUSH AX
	MOV AH,1
	INT 21H
	CMP AL, 'a'
	JB OVERFLOW
	CMP AL, 'z'
	JA OVERFLOW
	SUB  AL,20H
	MOV DL,AL
	MOV AH,06H
	INT 21H
	OVERFLOW: RET
	MAIN  ENDP
	CODE ENDS
END START
